name: cumulus
description: A server-like experience for AWS Lambda
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.17.5 <3.0.0'

dependencies:
  aws_lambda_dart_runtime: ^1.1.0
  dotenv: ^4.0.1

dev_dependencies:
  build_runner:
  shelf: ^1.3.1
  test: ^1.21.4
  lints: ^2.0.0